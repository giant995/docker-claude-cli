services:
  claude:
    build: .
    volumes:
      - ${HOST_DIR:-.}:/workspace
      - claude-config:/root/.claude
    working_dir: /workspace
    stdin_open: true
    tty: true

volumes:
  claude-config:
