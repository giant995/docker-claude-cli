# Your Anthropic API key - get one at https://console.anthropic.com
ANTHROPIC_API_KEY=your_api_key_here

# Optional: override the host directory to mount (defaults to current directory)
# HOST_DIR=/path/to/your/project
